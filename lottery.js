import { EmbedBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle } from 'discord.js';

/*
let participants = new Set();

export default async function newLottery(client, interaction) {

    const participate = new ButtonBuilder()
        .setCustomId('participate')
        .setLabel('Участвовать')
        .setStyle(ButtonStyle.Success)

    const embed = new EmbedBuilder()
        .setColor(0x0099ff)
        .setTitle('🎉 Розыгрыш!')
        .setDescription(description)
        .setFooter({ text: 'Участвуйте сейчас!' });

    const row = new ActionRowBuilder().addComponents(participate);
    const channel = client.channels.cache.get(process.env.CHANNEL_ID);

    channel.send({ content: 'Новый розыгрыш начался!', embeds: [embed], components: [row] })

} */